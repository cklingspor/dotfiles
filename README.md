# Dotfiles

## Installation

Run the install script to set up all symlinks and install dependencies:
```bash
./install.sh
```

This will symlink:
- Starship prompt configuration (`.config/starship.toml`)
- Ghostty terminal configuration
- Shell configuration (`.zshrc`)
- Vim configuration (`.vimrc`)
- VSCode settings and keybindings
